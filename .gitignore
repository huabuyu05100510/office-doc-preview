# 依赖（体积大，可重装）
node_modules/
**/node_modules/

# 运行数据：上传的原文件、转码/栅格化产物、任务元数据（含用户隐私，勿入库）
office-preview-app/.data/

# 大体积样本（164MB PDF、视频、音频）——按需放本地，不入库
files/*.pdf
files/*.mp4
files/*.mp3
files/*.pptx
files/*.ppt

# 个人简历 / 含手机号邮箱的敏感样本（公开仓库防泄露）
files/*郭亚平*
files/*GuoYaping*
files/*Resume*
files/*resume*

# 系统垃圾
.DS_Store
Thumbs.db
*.log
